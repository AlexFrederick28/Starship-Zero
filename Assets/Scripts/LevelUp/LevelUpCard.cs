using UnityEngine;

public class LevelUpCard : MonoBehaviour
{

    // potential use, here just in case

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
